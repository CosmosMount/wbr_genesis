# wbr_genesis
reinforcement learning &amp; simulation with genesis
## set-up
1. download the latest release of genesis
```bash
cd genesis
pip install -e . "[dev]"
```
to support usd
```bash
pip install genesis-world[usd]
```
2. install torch following the official instruction or
```bash
pip install torch
```